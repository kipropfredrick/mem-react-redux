import { combineReducers } from "redux";
import posts from './posts.js'
export default combineReducers({
    //from reducers 
    //if value and the key are same we keep the key
   //posts:posts,
   posts, 
});